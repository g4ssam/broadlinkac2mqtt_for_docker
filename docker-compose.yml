    version: '3.5'
    services:
      broadlinkac2mqtt:
        image: "ghcr.io/REMOVED_USER/broadlinkac2mqtt_for_docker/tree/Test_Simon_branch"
        container_name: "broadlinkac2mqtt_Simon"
        restart: "on-failure"
        volumes:
            - /var/lib/docker/volumes/broadlink_config/_data:/config